import { Page } from "puppeteer";
export declare class KeyCombination {
    private page;
    private keys;
    /**
     * @param keyString Examples:
     * - [Enter]
     * - [Ctrl+Shift+A]
     * - ArrowLeft
     */
    constructor(page: Page, keyString: string);
    /**
     * Type key combination in currently focused element.
     */
    type(delayMs?: number): Promise<void>;
    private isKeyboardKey;
    private wait;
}
